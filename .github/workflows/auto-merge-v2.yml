# Disabled: old dependabot auto-merge; maintainer scripts handle bulk merges.
name: Auto-Merge PRs v2

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  enable-auto-merge:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-merge v2 disabled — use maintainer merge scripts or manual review."
